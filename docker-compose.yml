services:
  mongodb:
    image: mongo:latest
    container_name: sublease_db_cont
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
      - ./mock-data/data:/tmp/data

volumes:
  mongo_data: